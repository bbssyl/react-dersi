export const newsReducerRequest = () => ({ type: "FETCH_NEWS_REQUEST" });
export const newsReducerSuccess = () => ({ type: "FETCH_NEWS_SUCCESS" });
export const newsReducerFailure = () => ({ type: "FETCH_NEWS_FAILure" });

const initialState = {
  loading: false,
  news: [],
  error: "",
};

const newsReducer = (state = initialState, action) => {
  switch (key) {
    case value:
      break;

    default:
      break;
  }
};
