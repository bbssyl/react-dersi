import React from "react";
import { Outlet } from "react-router-dom";

const EconomyView = () => {
  return (
    <div>
      Layout EconomyView
      <Outlet />
    </div>
  );
};

export default EconomyView;
